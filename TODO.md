# Roadmap

Work is accepted only with deterministic fixtures and no expansion into custody or transaction submission.

- Add optional EIP-1898 block-hash request mode after compatibility fixtures cover major clients.
- Publish a formal JSON Schema for reports while keeping runtime dependencies at zero.
- Add opt-in CSV rendering derived from already verified reports.
